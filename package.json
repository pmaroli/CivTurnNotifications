{
  "name": "civdataparser",
  "version": "1.0.0",
  "description": "A server that handles POST requests for Civ and converts them into a format that can be sent to a Discord Bot.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index"
  },
  "keywords": [],
  "author": "pmaroli",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "joi": "^14.3.1",
    "nodemailer": "^5.1.1"
  },
  "devDependencies": {}
}
